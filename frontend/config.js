// Local dev: points at the Flask backend running on your machine (python app.py).
// For production, change this to your deployed backend's HTTPS URL instead.
window.API_BASE_URL = "https://planfinder-4chw.onrender.com";
